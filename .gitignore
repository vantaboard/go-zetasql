# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.proto
# Test binary, built with `go test -c`
*.test
*.py
*.template
*.textproto
*.a
*.csv
*.y
*.l
*.input_file
*.bzl
full_export.inc

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

*.txt
*.cmake

# GoogleSQL C++ bindings; populated by installer or build-from-source (cmd/updater)
internal/ccall

# GoogleSQL upstream; cloned at build time in cmd/updater, never committed
cmd/updater/googlesql
